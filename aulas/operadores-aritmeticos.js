let x = 10;

// x = x + 3;
// x += 3;

// x = x - 3;
// x -= 3;

// x = x * 3;
// x *= 3;

// x = x / 3;
// x /= 3;

// x = x % 5 -> modulo

//Incrementação:
// x++;   soma mais 1 no valor da variavel x
// x--;   subtrai menos 1 no valor da variavel x

// quando se utiliza variavel++ é inicialmente utilizado o conteúdo da variável e depois incrementada a variável.
// quando se utiliza ++variavel é inicialmente incrementada a variável e depois utilizado o conteúdo

// let x = 10;
// let y = x++;
//resultado: x = 11 e y = 10
//Porque primeiro o valor inicial do x é armazenada dentro do y
// depois acontece a concatenação com x

//outraforma:
//let x = 10;
//let y = ++x;
//resultado: x = 11 e y= 11;
//Porque o x já inicia incrementado e o y recebe o valor do x já incrementado

console.log(x);
